var creepController = require('creepController');

/**
 *
 * Data for room
 */
var roomController = {
    run: function(spawn){
        creepController.run(spawn);
    }
};

module.exports = roomController;